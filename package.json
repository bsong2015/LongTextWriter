{
  "name": "gendoc-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/cli",
    "packages/web-ui",
    "packages/shared",
    "e2e-tests"
  ],
  "scripts": {
    "build": "npm run build --workspace=@gendoc/shared && npm run build --workspace=@gendoc/web-ui && npm run build --workspace=@gendoc/cli",
    "dev:backend": "npm run build --workspace=@gendoc/shared && npm run start:web --workspace=@gendoc/cli",
    "dev:frontend": "npm run dev --workspace=@gendoc/web-ui",
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "start:web": "npm-run-all --parallel dev:backend dev:frontend",
    "package": "npm run build && npm pack --workspace=@gendoc/cli"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "npm-run-all": "^4.1.5"
  }
}
